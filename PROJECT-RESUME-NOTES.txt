BOITEKONG PULSE - PROJECT RESUME NOTES
Date: 2026-04-20

PURPOSE OF THIS FILE
- This is the quickest resume point for the next session.
- It reflects the codebase more accurately than the older "backend scaffold only" description.
- If Codex loses context later, start from this file first.

PROJECT NAME
- Boitekong Pulse

PROJECT GOAL
- Boitekong Pulse is a mobile-first township community web app.
- It focuses on local updates, comments, replies, direct messages, voice notes, notifications, search, and reporting.
- Product direction remains: local, township-first, voice-friendly, and community-centered.

CURRENT APP STATE
- The app is no longer just a frontend-only prototype.
- It is now a hybrid web app with a real backend plus frontend caching.
- The frontend service layer is already API-backed for:
  - auth
  - user profile/settings/search/follow/block
  - posts/comments/reactions
  - conversations/messages
  - notifications
  - reports
- Browser localStorage is still used for:
  - access token/session persistence
  - cached users/posts/conversations/notifications/reports
  - hidden-report state
  - direct-message encryption keys
  - lightweight UI preferences
- Backend now includes:
  - Express app
  - MongoDB/Mongoose
  - JWT auth
  - SMS OTP flow
  - posts/comments API
  - conversation/message API
  - notifications API
  - reports API
  - realtime websocket sync at `/api/realtime`
- Browser notifications exist, but full push-notification infrastructure is NOT done.
- Voice notes are stored through the current Mongo/base64-binary migration flow for now.
- Profile avatar uploads are no longer kept as big data URLs in the main profile payload:
  - avatar upload/delete is API-backed
  - avatar files are stored on the server and served from `/uploads/avatars`
  - frontend now prefers `avatarUrl` and syncs the current user from the API after avatar changes
- Dedicated media/object storage is still a later step.

CURRENT PHASE
- We are in late Phase 7.
- Phase 7 is no longer just "scaffold the backend."
- Phase 7 now means:
  - finish migration cleanup
  - run real end-to-end QA
  - verify SMS OTP on a real phone
  - prepare the app for a controlled beta environment

INTERNAL STATUS SUMMARY
- UI feature set is mature.
- Main API slices are wired.
- Live sync exists.
- Remaining work is mostly verification, cleanup, and production hardening.
- A lot of recent work has shifted from feature building to UX tightening, duplicate-protection, and launch cleanup.

MAIN FRONTEND ROUTES
- login
- register
- feed
- profile
- create-post
- edit-post
- search
- messages

PRODUCTION BATCH TARGETS
- Use these list batching values for production:
  - FEED_BATCH_SIZE = 10
  - COMMENT_BATCH_SIZE = 6
  - REPLY_BATCH_SIZE = 4
  - SEARCH_BATCH_SIZE = 10
  - CHAT_BATCH_SIZE = 10
  - NOTIFICATION_BATCH_SIZE = 10
  - DISCOVER_USERS_BATCH_SIZE = 10
  - THREAD_MESSAGE_BATCH_SIZE = 24

SHARING + DISTRIBUTION NOTES
- Share API flows should include the deployed web-app production origin in shared content whenever the app is running from a real HTTPS/non-localhost origin.
- Post/comment/invite share fallbacks should copy the same app URL into the clipboard text when native share is unavailable.
- If the browser supports Web Share file payloads, attach the branded app logo asset with the share.
- A QR code with embedded logo is still an optional follow-up enhancement; there is no dedicated QR generator wired into the repo yet.

PUBLIC INFO COPY SOURCES
- The public-facing copy for:
  - About
  - Help
  - Privacy
  - Terms & Conditions
  - Contact
  - Install / PWA
  lives in:
  - `js/views/publicInfoView.js`
- Those pages still need a final production content pass rather than placeholder/product-direction copy only.

BACKEND AUTH / KEY NOTES
- Backend environment/auth configuration is currently centered in `server/src/config/env.js`.
- Frontend split-domain deployment configuration now also depends on `runtime-config.js` at the repo root.
- The frontend reads `window.BOITEKONG_PULSE_CONFIG.API_BASE_URL` before boot.
- Leave that blank only for localhost/private-network development.
- For separate frontend/API domains in production, set it to the full API URL, for example:
  - `https://your-api.onrender.com/api`
- Important auth/security-related env values to confirm before production:
  - `JWT_SECRET`
  - `JWT_EXPIRES_IN`
  - `CORS_ORIGIN`
  - `SMS_APIKEY` / `SMS_API_KEY`
- `JWT_SECRET` already throws in production if it is left at the default fallback value.
- `CORS_ORIGIN` now also throws in production if it is blank, wildcard, or not a valid origin list.
- Backend hardening now also includes:
  - security headers on API responses
  - `no-store` cache headers on `/api`
  - auth/signup/password-reset/OTP rate limits
  - websocket upgrade origin checks plus handshake throttling
- `TRUST_PROXY` should be set correctly when the API is behind Render or another reverse proxy.
- Backend now also supports an optional partitioned MongoDB layout:
  - `MONGODB_URI` remains the core store for users, OTP verifications, and reports
  - `MONGODB_URI_TWO` can host posts and comments
  - `MONGODB_URI_THREE` can host conversations and messages
  - `MONGODB_URI_FOUR` can host notifications
  - `MONGODB_PARTITION_MODE=single` keeps all reads/writes on the primary DB
  - `MONGODB_PARTITION_MODE=partitioned` activates the split bindings after data has been copied
- Safe rollout for multi-DB mode:
  - keep `MONGODB_PARTITION_MODE=single`
  - set `MONGODB_URI_TWO` / `MONGODB_URI_THREE` / optional `MONGODB_URI_FOUR`
  - run `npm run db:migrate-partitions`
  - verify the copied data
  - only then switch to `partitioned`
- The partition migration script is copy-first and does NOT delete the original collections yet.
- Production deployment still needs real secret rotation and confirmation that no default/local values remain in use.

IMPORTANT STORAGE KEYS STILL IN USE
- USERS: bp_users
- POSTS: bp_posts
- CURRENT_USER: bp_current_user
- ACCESS_TOKEN: bp_access_token
- DIRECT_MESSAGE_E2E_KEYS: bp_direct_message_e2e_keys
- CONVERSATIONS: bp_conversations
- NOTIFICATIONS: bp_notifications
- REPORTS: bp_reports
- CREATE_POST_BRIEF_DISMISSED: bp_create_post_brief_dismissed

WHAT IS ALREADY BUILT

1. AUTH + ACCOUNT
- Register and login use the API.
- Login accepts username or phone number.
- Register requires phone number.
- Forgot password now exists from the login page:
  - user enters the registered phone number
  - SMS reset OTP is sent only if that number exists in the database
  - reset OTP expires after 5 minutes
  - forgot-password reset can only be completed once every 24 hours
  - successful reset returns the user to the login page
- Profile update uses the API.
- Phone verification flow exists in code:
  - send OTP
  - verify OTP
- Direct-message encryption key sync exists.

2. PROFILE + COMMUNITY GRAPH
- Profile page is built.
- Edit profile is built.
- Profile photo upload/replace/remove is API-backed.
- Current profile photo can be viewed in a larger viewer and deleted from there.
- Profile photo uploader now uses the same compact add-image icon pattern as post/edit-post upload.
- Follow/unfollow is API-backed.
- Followers/following lists are API-backed.
- DM and notification settings are API-backed.
- Block/unblock is API-backed.

3. FEED / POSTS / COMMENTS
- Feed loads from the API.
- Create post is API-backed.
- Edit/delete post is API-backed.
- Client-side duplicate-submit protection exists for post creation.
- Server-side duplicate-submit protection also exists for post creation.
- Post reactions are API-backed.
- Comments/replies are API-backed.
- Comment reactions are API-backed.
- Voice-note posts/comments/replies are supported in the current migration flow.
- Voice-note posts can now include one image.
- Create-post and edit-post now use compact image-picker UI instead of the older upload panel UI.
- "See more posts" scroll behavior was fixed so loading more content no longer forces the user to start over from the top.

4. DIRECT MESSAGES
- Conversations load from the API.
- Direct conversation creation is API-backed.
- Send/edit/delete message is API-backed.
- Client-side duplicate-submit protection exists for DM send.
- Server-side duplicate-submit protection also exists for DM send.
- Voice-note messages are supported.
- Message reply UI exists:
  - action-sheet reply entry
  - swipe-to-reply flow
  - reply target preview before send
- Conversation archive/delete-from-inbox behavior is API-backed.
- Read state is API-backed.
- End-to-end encryption session handling exists on the frontend.

5. NOTIFICATIONS
- Notification list is API-backed.
- Mark one/all as read is API-backed.
- Conversation notifications can be cleared/read.
- Browser notifications are wired for supported browsers.
- Notification click routing is wired in the app.
- Notification list now supports removing individual notifications.
- Notification API payloads include the app logo.
- Message notifications can open the exact chat.
- Comment/reaction notifications can route into the feed and focus the related post.

6. SEARCH
- User search is API-backed.
- Post search is API-backed.
- Search screens still use cache as a safe fallback if refresh fails.

7. REPORTING
- Report submission is API-backed.
- Duplicate report protection exists.
- Hide-for-me flow exists.
- Hidden reported content is filtered in feed/comments/notifications for the reporting user.
- Moderator UI is still postponed.

8. LIVE SYNC
- Polling plus websocket live sync exists.
- Feed can opt into live post refresh.
- Messages and notifications can refresh from realtime events.

9. BACKEND TOOLING
- `npm run dev:api`
- `npm run start:api`
- `npm run check:api`
- `npm run db:init`
- `npm run check:db`
- `npm run db:migrate-partitions`
- `npm run db:cleanup-partitions`
- `npm run cleanup:legacy-voice-notes`

WHAT WAS CLEANED UP TODAY
- Migrated profile avatar handling away from large cached data URLs to API-backed upload/delete plus server-hosted avatar files.
- Fixed stale avatar refresh/sync bugs so the new avatar URL is used immediately after profile changes.
- Tightened DM availability state so enabling/disabling DMs updates search/profile/messages UI immediately.
- Added DM reply interactions and improved related DM action-sheet UX.
- Added duplicate-submit protection for both DMs and posts on client and server sides.
- Added notification polish:
  - app logo in notification payloads
  - clear individual notifications
  - open exact chat from message notifications
  - open/focus related post from comment/reaction notifications
- Fixed "see more" loading behavior for posts.
- Reworked create-post, edit-post, and profile-photo image upload UI into a compact icon-based pattern.
- Removed the now-unused older post image upload panel path/styles.
- Allowed voice-note posts to include one image.

NEXT MAIN TASKS
- Run real mobile QA on Friday afternoon, 2026-04-17, focusing on the flows changed recently.
- Fix any spacing, tap-target, upload, voice-note, or notification routing issues found during that device pass.
- After mobile QA, run a final production-readiness checklist rather than assuming "tested" equals "production-ready."

RECOMMENDED QA ORDER
1. Register
2. Login
3. Forgot password request by registered phone number
4. Forgot password OTP confirm within 5 minutes
5. Edit profile
6. Profile photo:
   - add photo
   - replace photo
   - remove pending selected photo before save
   - open viewer
   - delete current photo from viewer
7. Send OTP
8. Verify OTP
9. Create text post
10. Create voice-note post
11. Create voice-note post with image
12. Edit/delete eligible post
13. Add comment/reply
14. React to post/comment
15. Search users/posts
16. Follow/unfollow
17. Block/unblock
18. Toggle DM allow/disable and confirm UI updates immediately
19. DM text
20. DM voice note
21. DM reply flow
22. Edit/delete message
23. Notification open/read/clear flows
24. Report post/comment with and without hide-for-me
25. Multi-tab or multi-device live sync sanity check

WHAT IS STILL LEFT BEFORE A REAL BETA GO-LIVE
- Real end-to-end QA needs to pass.
- Real SMS OTP needs to pass with the live provider/device flow.
- Real mobile-device QA still needs to pass for the recently updated upload/voice-note/DM/notification flows.
- Production environment values must be set correctly:
  - JWT secret
  - CORS origin
  - Mongo connection
  - SMS API key
- MongoDB Atlas/network access must be confirmed from the deployment environment.
- Backup and restore expectations should be defined for Mongo.
- Backup/retention expectations should also be defined for uploaded avatar files.
- Monitoring/logging should be in place for the deployed API.
- Auth and OTP abuse controls should be confirmed before public exposure.
- PWA/service-worker behavior should be retested after backend deployment.
- Legal/privacy/minimum public-facing product copy still needs final content if this will be publicly accessible.
- Public info pages still need a final production wording pass across About, Help, Privacy, Terms & Conditions, Contact, and Install/PWA.
- Backend auth/security env values still need a final production verification pass, especially JWT signing, CORS, and SMS provider keys.

BETA GO-LIVE DECISION RULE
- If Friday/mobile QA passes, that means the product flow is in a strong place.
- It does NOT automatically mean "production beta live" is fully safe.
- For a controlled beta, the app is close.
- For a public production beta, also confirm:
  - deploy environment works
  - secrets are production-safe
  - CORS is locked correctly
  - SMS OTP works live
  - monitoring exists
  - basic abuse/rate-limit protection is covered
  - rollback/recovery plan exists

WHAT IS NOT IN SCOPE RIGHT NOW
- Moderator dashboard
- Moderator decision tools
- Full admin tooling
- Dedicated object storage/media CDN
- Full web push notification system
- Full legal/install/marketing content
- Final pixel-perfect responsive pass on every viewport

MOST LIKELY NEXT MILESTONE LABELS
- v0.7.x = migration cleanup + QA + OTP verification
- v0.8 = controlled beta hardening + deployment prep
- v1.0-beta = live beta after environment + ops + QA gates are satisfied

GOOD NEXT PROMPTS
- "Resume from PROJECT-RESUME-NOTES.txt and help me run the QA checklist."
- "Resume from PROJECT-RESUME-NOTES.txt and debug the OTP flow."
- "Resume from PROJECT-RESUME-NOTES.txt and harden the app for beta deploy."
- "Resume from PROJECT-RESUME-NOTES.txt and update the deployment checklist."
- "Resume from PROJECT-RESUME-NOTES.txt and help me work through the Friday mobile QA pass."

FINAL RECOMMENDATION
- Friday should focus on real mobile QA plus OTP verification, not new features.
- If those pass, the app is very close to controlled beta, but still run the production-readiness checklist before launch.
- Do not treat "feature complete" as the same thing as "production safe."
